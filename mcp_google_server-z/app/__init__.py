# This package contains the FastAPI application and supporting modules for the
# Google Tools MCP server. It is intentionally split into smaller modules to
# keep concerns separated: configuration handling, Google API authentication,
# individual tool implementations, and the registry that binds them together.
